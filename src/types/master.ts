export type DrinkDefinition = {
  key: string
  label: string
  display_order: number
}

export type GenericMaster = {
  id: string
  key: string
  label: string
  display_order: number
  table: string
}